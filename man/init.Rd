% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/init.R
\name{init}
\alias{init}
\title{Initialize an intent Project}
\usage{
init(
  path = ".",
  repos = NULL,
  r_version = NULL,
  install_self = "hydrate",
  confirm_repos = interactive()
)
}
\arguments{
\item{path}{Character string. Path to the new project directory. Defaults to current working directory.}

\item{repos}{Character vector. Named repositories to use. Defaults to
the value of the \code{INTENT_DEFAULT_REPOS} environment variable (format:
\code{"NAME=URL,NAME=URL"}), or the CRAN mirror at
\url{https://cran.r-project.org} when the variable is
unset. Set the variable to an empty string to require explicit
repository configuration on every init.}

\item{r_version}{R version constraint to write into \code{DESCRIPTION} as
\verb{Depends: R (>= x.y)}.  Use \code{NULL} (the default) to record the
current session's major.minor version, or \code{NA} to skip writing an R
version constraint.}

\item{install_self}{Character string. \code{"hydrate"} attempts to copy the
currently installed \code{intent} package from the active library paths into the
new project library. \code{"never"} leaves \code{intent} as an external tool only.}

\item{confirm_repos}{Logical. If \code{TRUE}, ask before writing the default
repository when no repository is configured. Defaults to \code{interactive()}.}
}
\description{
Sets up a directory as an \code{intent} project. \code{init()} first classifies the
existing \code{DESCRIPTION} file, if any, before mutating project files. Existing
projects must already have an intent-compliant \code{DESCRIPTION} with
\verb{Config/intent/repos/} fields; otherwise init stops before rewriting
\code{renv.lock}, \code{.Rprofile}, \code{.Renviron}, or the project library.
}
\details{
For new empty projects, \code{init()} creates \code{DESCRIPTION}, initializes a bare
\code{renv} environment, sets the snapshot type to "explicit", and configures
\code{.Rprofile} and \code{.Renviron}. The bootstrap dependency on \code{intent} is written
with a default lower-bound version constraint. Minimum \code{pak} and \code{renv}
constraints are copied from the running \code{intent} package metadata when
declared there; existing user constraints are preserved.
}
